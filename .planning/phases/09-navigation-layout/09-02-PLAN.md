---
phase: 09-navigation-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
    - src/lib/components/organisms/Footer.svelte
autonomous: true

must_haves:
    truths:
        - 'Footer has no 120px empty gap'
        - 'Footer displays copyright left, social icons right'
        - 'Footer stacks vertically on mobile'
    artifacts:
        - path: 'src/lib/components/organisms/Footer.svelte'
          provides: 'Minimal horizontal footer layout'
          min_lines: 30
    key_links:
        - from: 'Footer.svelte'
          to: 'Socials component'
          via: 'icon-only socials'
          pattern: 'Socials'
---

<objective>
Remove footer empty space and create horizontal copyright/socials layout.

Purpose: Footer adds value without wasted vertical space. Clean, professional finish. Output:
Minimal footer with copyright left, social icons right, responsive stacking. </objective>

<execution_context> @./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-navigation-layout/09-CONTEXT.md
@.planning/phases/09-navigation-layout/09-RESEARCH.md
@src/lib/components/organisms/Footer.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove empty space, restructure to horizontal</name>
  <files>src/lib/components/organisms/Footer.svelte</files>
  <action>
Remove the grid layout causing 120px empty space:
- Delete `grid-template-rows: 120px 1fr`
- Change to simple padding: `padding: 24px 0`

Restructure .content to horizontal flexbox:

- `flex-direction: row` (horizontal)
- `justify-content: space-between` (copyright left, socials right)
- `align-items: center`
- Add container constraints: `max-width: 1080px`, `margin: 0 auto`, `padding: 0 15px`

Add mobile responsive stacking:

```scss
@media (max-width: 767px) {
    flex-direction: column;
    gap: 16px;
}
```

Update .legal section:

- Add copyright year dynamically: `© {new Date().getFullYear()}`
- Display as flex row with gap between Impressum and copyright

Update .socials section:

- Keep existing Socials component and RssLink
- Ensure horizontal layout with gap </action> <verify> Run `npm run dev` and scroll to footer.
  Verify: No large empty gap above footer content. Verify: Copyright/Impressum on left, social icons
  on right. Resize to mobile: Copyright stacks above socials. </verify> <done> Footer has 24px
  padding, no 120px gap. Horizontal layout on desktop, stacked on mobile. Copyright displays with
  dynamic year. </done> </task>

<task type="auto">
  <name>Task 2: Add copyright text and polish</name>
  <files>src/lib/components/organisms/Footer.svelte</files>
  <action>
In the .legal div, add copyright span:
```svelte
<div class="legal">
    <a href={resolve('/impressum')}>Impressum</a>
    <span class="copyright">© {new Date().getFullYear()}</span>
</div>
```

Style .legal:

- `display: flex`
- `align-items: center`
- `gap: 16px`
- `font-size: 0.9rem` (slightly smaller, subtle)

Ensure subtle top border matches existing:

- Keep `border-top: 1px solid var(--color--waves-start)`

Keep existing hover effect on links:

- `filter: drop-shadow(0px 0px 3px var(--color--primary))` </action> <verify> Run `npm run check` -
  no errors. Visual check: Footer shows "Impressum © 2026" on left, social icons on right. Hover
  over links - orange glow effect works. </verify> <done> Footer displays copyright with current
  year. Legal section has proper styling. Links have hover glow effect. </done> </task>

</tasks>

<verification>
Visual verification:
1. Footer has minimal vertical padding (24px), no large empty gap
2. Desktop: Impressum/copyright left, social icons right
3. Mobile: Content stacks centered
4. Hover effects work on all links

Code verification:

- `npm run check` passes
- `npm run lint` passes </verification>

<success_criteria>

- [ ] No 120px empty space above footer content
- [ ] Horizontal layout: copyright left, socials right
- [ ] Mobile: stacked layout
- [ ] Copyright shows current year
- [ ] No TypeScript or lint errors </success_criteria>

<output>
After completion, create `.planning/phases/09-navigation-layout/09-02-SUMMARY.md`
</output>
