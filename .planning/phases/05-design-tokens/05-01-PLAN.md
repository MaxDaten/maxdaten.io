---
phase: 05-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    - src/lib/scss/_tokens-spacing.scss
    - src/lib/scss/_tokens-typography.scss
    - src/lib/scss/_tokens-colors.scss
    - src/lib/scss/global.scss
autonomous: true

must_haves:
    truths:
        - 'Spacing scale CSS properties exist and use 8px base grid'
        - 'Typography scale CSS properties exist using Major Third 1.25 ratio'
        - 'Color tokens define single accent color (orange, no teal)'
        - 'Semantic tokens exist for common use cases'
        - 'Site looks identical before and after (tokens defined but not consumed)'
    artifacts:
        - path: 'src/lib/scss/_tokens-spacing.scss'
          provides: 'Spacing primitives and semantic tokens'
          contains: '--raw-space-8'
        - path: 'src/lib/scss/_tokens-typography.scss'
          provides: 'Typography primitives and semantic tokens'
          contains: '--raw-text-base'
        - path: 'src/lib/scss/_tokens-colors.scss'
          provides: 'Color primitives and semantic tokens'
          contains: '--color-accent'
        - path: 'src/lib/scss/global.scss'
          provides: 'Token file imports'
          contains: '_tokens-spacing'
    key_links:
        - from: 'src/lib/scss/global.scss'
          to: 'src/lib/scss/_tokens-*.scss'
          via: '@use imports'
          pattern: "@use '_tokens-"
        - from: ':root'
          to: 'CSS custom properties'
          via: 'property definitions'
          pattern: '--raw-space|--raw-text|--color-'
---

<objective>
Establish the CSS custom property foundation for the design system.

Purpose: Create spacing, typography, and color tokens that all subsequent styling (Phases 6-9) will
derive from. This is an additive phase - tokens are defined but not yet consumed by components.

Output: Three new SCSS token files imported into global.scss, with all CSS custom properties
available in `:root`. </objective>

<execution_context> @./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-design-tokens/05-CONTEXT.md
@.planning/phases/05-design-tokens/05-RESEARCH.md
@src/lib/scss/global.scss
@src/lib/scss/_themes.scss
@src/lib/scss/_variables.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create token files</name>
  <files>
    src/lib/scss/_tokens-spacing.scss
    src/lib/scss/_tokens-typography.scss
    src/lib/scss/_tokens-colors.scss
  </files>
  <action>
Create three new SCSS files defining CSS custom properties in `:root`.

**\_tokens-spacing.scss:**

```scss
:root {
    // Primitive spacing (strict 8px grid)
    --raw-space-8: 8px;
    --raw-space-16: 16px;
    --raw-space-24: 24px;
    --raw-space-32: 32px;
    --raw-space-48: 48px;
    --raw-space-64: 64px;
    --raw-space-80: 80px;

    // Semantic spacing
    --space-inline: var(--raw-space-8);
    --space-block: var(--raw-space-24);
    --space-section: var(--raw-space-48);
    --space-page: var(--raw-space-80);
}
```

**\_tokens-typography.scss:**

```scss
:root {
    // Primitive typography - Major Third scale (1.25 ratio)
    --raw-text-xs: 12px;
    --raw-text-sm: 14px;
    --raw-text-base: 16px;
    --raw-text-md: 18px;
    --raw-text-lg: 20px;
    --raw-text-xl: 25px;
    --raw-text-2xl: 31px;
    --raw-text-3xl: 39px;
    --raw-text-4xl: 49px;

    // Primitive line-heights
    --raw-leading-tight: 1.1;
    --raw-leading-snug: 1.25;
    --raw-leading-normal: 1.5;
    --raw-leading-relaxed: 1.65;

    // Font weights
    --font-weight-normal: 400;
    --font-weight-medium: 500;
    --font-weight-semibold: 600;
    --font-weight-bold: 700;

    // Semantic typography
    --text-body: var(--raw-text-base);
    --text-body-leading: var(--raw-leading-relaxed);
    --text-small: var(--raw-text-sm);
    --text-caption: var(--raw-text-xs);
    --text-heading-1: var(--raw-text-3xl);
    --text-heading-2: var(--raw-text-2xl);
    --text-heading-3: var(--raw-text-xl);
    --text-heading-4: var(--raw-text-lg);
    --text-heading-leading: var(--raw-leading-tight);
}
```

**\_tokens-colors.scss:**

```scss
:root {
    // Primitive colors - Orange accent (no teal)
    --raw-color-orange-400: #ff9933;
    --raw-color-orange-500: #ff8000;
    --raw-color-orange-600: #e67300;
    --raw-color-orange-700: #cc6600;
    --raw-color-orange-warm: #ffb347;

    // Primitive grays/surfaces
    --raw-color-gray-900: #1c1e26;
    --raw-color-gray-850: #141519;
    --raw-color-gray-800: #32343e;
    --raw-color-gray-700: #2b3131;

    // Primitive text colors
    --raw-color-white: #fffcfc;
    --raw-color-white-muted: #d9f9fd;

    // Semantic colors
    --color-accent: var(--raw-color-orange-500);
    --color-accent-hover: var(--raw-color-orange-600);
    --color-text: var(--raw-color-white);
    --color-text-muted: rgba(255, 252, 252, 0.7);
    --color-surface: var(--raw-color-gray-900);
    --color-surface-elevated: var(--raw-color-gray-800);
    --color-border: rgba(255, 128, 0, 0.3);
}
```

Note: Use exact values from 05-RESEARCH.md. Add a comment header to each file indicating it's part
of the design token system. </action> <verify> Files exist and contain expected content:

- `ls src/lib/scss/_tokens-*.scss` shows 3 files
- Each file contains `:root {` block
- Spacing file contains `--raw-space-8` through `--raw-space-80`
- Typography file contains `--raw-text-xs` through `--raw-text-4xl`
- Colors file contains `--color-accent` and NO teal/secondary colors </verify> <done> Three token
  files created with complete primitive and semantic token definitions following the two-layer
  architecture. </done> </task>

<task type="auto">
  <name>Task 2: Update global.scss imports</name>
  <files>src/lib/scss/global.scss</files>
  <action>
Add imports for the new token files in global.scss. Import order matters - tokens must come after reset and before themes.

Update the import section to:

```scss
@use '_reset.scss';
@use '_variables.scss';
@use '_tokens-spacing.scss';
@use '_tokens-typography.scss';
@use '_tokens-colors.scss';
@use '_themes.scss';
// ... rest unchanged
```

The token files should be imported:

1. After `_reset.scss` (reset needs to run first)
2. After `_variables.scss` (contains font family definitions used by typography)
3. Before `_themes.scss` (themes may reference tokens in future)

Do NOT modify any other content in global.scss. Only add the three @use statements. </action>
<verify> Run `npm run build` - should complete without errors. Run `npm run check` - should pass
svelte-check. </verify> <done> global.scss imports the three token files in correct order, build
succeeds. </done> </task>

<task type="auto">
  <name>Task 3: Verify tokens available</name>
  <files>None (verification only)</files>
  <action>
Start dev server and verify tokens are available in browser DevTools.

1. Run `npm run dev`
2. Open http://localhost:5173 in browser
3. Open DevTools > Elements > select `html` or `:root`
4. In Computed/Styles panel, filter for custom properties

Verify presence of:

- Spacing: `--raw-space-8`, `--raw-space-16`, `--space-block`, `--space-section`
- Typography: `--raw-text-base`, `--raw-text-xl`, `--text-body`, `--text-heading-1`
- Colors: `--raw-color-orange-500`, `--color-accent`, `--color-text`, `--color-surface`

Also verify:

- Site renders correctly (no visual changes from before)
- No console errors related to CSS
- Existing styles still work (colors, fonts, layout unchanged) </action> <verify> Visual inspection:
  Site looks identical to before this plan. DevTools: All token CSS custom properties visible on
  `:root`. Console: No CSS-related errors. </verify> <done> Tokens are available as CSS custom
  properties, site appearance unchanged, ready for consumption in Phases 6-9. </done> </task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run check` passes
3. Three new files exist in `src/lib/scss/`
4. `global.scss` imports all three token files
5. DevTools shows all CSS custom properties on `:root`
6. Site visual appearance unchanged (tokens defined but not consumed)
</verification>

<success_criteria>

- TOKEN-01 satisfied: Spacing tokens exist with 8px grid (8, 16, 24, 32, 48, 64, 80)
- TOKEN-02 satisfied: Typography tokens exist with Major Third scale (12-49px)
- TOKEN-03 satisfied: Color tokens use orange accent only (no teal)
- TOKEN-04 satisfied: Semantic tokens exist (--space-block, --text-body, --color-accent, etc.)
- Site looks identical before and after (additive change only) </success_criteria>

<output>
After completion, create `.planning/phases/05-design-tokens/05-01-SUMMARY.md`
</output>
