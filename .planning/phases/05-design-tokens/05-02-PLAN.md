---
phase: 05-design-tokens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
    - src/lib/scss/_tokens-spacing.scss
    - src/lib/scss/_themes.scss
autonomous: true
gap_closure: true

must_haves:
    truths:
        - 'Spacing scale includes all values from TOKEN-01: 4, 8, 12, 16, 24, 32, 48, 64, 80px'
        - 'No teal color visible on site - all accent elements display orange'
        - 'Build passes with no SCSS errors'
    artifacts:
        - path: 'src/lib/scss/_tokens-spacing.scss'
          provides: 'Complete spacing scale including 4px and 12px'
          contains: '--raw-space-4: 4px'
        - path: 'src/lib/scss/_themes.scss'
          provides: 'Orange-only color system'
          contains: '$color-secondary: #ff8000'
    key_links:
        - from: '_themes.scss'
          to: 'components via --color--secondary'
          via: 'define-color mixin'
          pattern: 'define-color.*secondary'
---

<objective>
Close verification gaps from Phase 05 by adding missing spacing tokens and consolidating the color system to orange-only.

Purpose: Complete TOKEN-01 (spacing scale) and TOKEN-03 (single accent color) requirements that were
partially addressed in 05-01.

Output: Updated token files that satisfy all Phase 5 success criteria. </objective>

<execution_context> @./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-design-tokens/05-VERIFICATION.md
@.planning/phases/05-design-tokens/05-01-SUMMARY.md
@src/lib/scss/_tokens-spacing.scss
@src/lib/scss/_themes.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing spacing tokens</name>
  <files>src/lib/scss/_tokens-spacing.scss</files>
  <action>
Add the two missing spacing values to complete the TOKEN-01 scale:

1. Add `--raw-space-4: 4px;` after the header comment block, before `--raw-space-8`
2. Add `--raw-space-12: 12px;` between `--raw-space-8` and `--raw-space-16`

The complete primitive spacing block should be:

```scss
// Primitive spacing (8px grid with half-steps)
--raw-space-4: 4px;
--raw-space-8: 8px;
--raw-space-12: 12px;
--raw-space-16: 16px;
--raw-space-24: 24px;
--raw-space-32: 32px;
--raw-space-48: 48px;
--raw-space-64: 64px;
--raw-space-80: 80px;
```

Update the header comment to note "8px grid with half-steps" since we now include 4px and 12px
values. </action> <verify> Run: `grep -E "raw-space-(4|12)" src/lib/scss/_tokens-spacing.scss`
Expected: Both `--raw-space-4: 4px` and `--raw-space-12: 12px` appear in output. </verify> <done>
Spacing token file contains all 9 values from TOKEN-01 scale: 4, 8, 12, 16, 24, 32, 48, 64, 80px
</done> </task>

<task type="auto">
  <name>Task 2: Consolidate secondary color to orange</name>
  <files>src/lib/scss/_themes.scss</files>
  <action>
Change the secondary color from teal to orange to consolidate to a single accent color system.

1. Update line 7: Change `$color-secondary: #0cd7f1;` to `$color-secondary: #ff8000;`
2. Update line 8: Change `$color-secondary-shade: #147b8c;` to `$color-secondary-shade: #7e4611;`
3. Update line 9: Change `$color-secondary-tint: #0d2a30;` to `$color-secondary-tint: #2d1d10;`

This makes `$color-secondary` an alias to `$color-primary`, achieving color consolidation while
maintaining backward compatibility with all components that use `--color--secondary` CSS variables.

The waves gradient colors (lines 102-103) use `--color--secondary-shade-rgb` which will
automatically become orange-based after this change.

Note: Site appearance WILL change - all teal elements become orange. This is the intended outcome of
TOKEN-03 (single accent color consolidation). </action> <verify> Run:
`grep "color-secondary:" src/lib/scss/_themes.scss | head -3` Expected: All three lines show orange
hex values (#ff8000, #7e4611, #2d1d10)

Run: `npm run build` Expected: Build completes successfully with no SCSS errors. </verify> <done>

- $color-secondary variables point to orange values
- All --color--secondary CSS custom properties now resolve to orange
- Build passes
- Site displays orange accent color everywhere (no teal) </done> </task>

</tasks>

<verification>
After both tasks complete:

1. **Spacing tokens complete:**

    ```bash
    grep "raw-space" src/lib/scss/_tokens-spacing.scss | wc -l
    ```

    Expected: 9 (all spacing values present)

2. **Color consolidation complete:**

    ```bash
    grep -E "0cd7f1|147b8c|0d2a30" src/lib/scss/_themes.scss
    ```

    Expected: No output (no teal hex codes remain)

3. **Build passes:**

    ```bash
    npm run build
    ```

    Expected: Successful build

4. **Visual verification (optional):** Run `npm run dev` and confirm accent elements (tags, buttons,
   code blocks) display orange instead of teal. </verification>

<success_criteria>

1. `_tokens-spacing.scss` contains --raw-space-4 and --raw-space-12
2. `_themes.scss` defines $color-secondary as orange (#ff8000)
3. No teal hex codes (#0cd7f1, #147b8c, #0d2a30) remain in \_themes.scss
4. Build passes successfully
5. All Phase 5 verification criteria now satisfied </success_criteria>

<output>
After completion, create `.planning/phases/05-design-tokens/05-02-SUMMARY.md`
</output>
