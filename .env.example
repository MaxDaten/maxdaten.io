# Sanity CMS Configuration
# Get your project ID from https://sanity.io/manage
PUBLIC_SANITY_PROJECT_ID=your-project-id
PUBLIC_SANITY_DATASET=production

# Sanity API token with write access
# Generate at https://sanity.io/manage -> API -> Tokens
# Required for: preview mode, content migration scripts
SANITY_API_TOKEN=your-write-token

# Optional: Preview mode secret
# Used for preview URLs like /?preview=your-secret
# Set to a random string to enable preview mode
SANITY_PREVIEW_SECRET=your-preview-secret
